<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Node.js Blog API Project</title>
  
  <!-- Bootstrap CSS (for UI styling) -->
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css" rel="stylesheet">

  <!-- Font Awesome for Icons -->
  <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0-beta3/css/all.min.css" rel="stylesheet">

  <!-- Custom Styling -->
  <style>
    body {
      font-family: Arial, sans-serif;
      background: #f9f9f9;
      padding: 30px;
      color: #333;
    }
    h1, h2 {
      color: #2c3e50;
    }
    code {
      background: #eaeaea;
      padding: 2px 6px;
      border-radius: 4px;
    }
    ul {
      padding-left: 20px;
    }
    li {
      margin-bottom: 8px;
    }
    section {
      margin-bottom: 2rem;
    }
    footer {
      margin-top: 3rem;
      font-weight: bold;
      color: #666;
    }
  </style>
</head>
<body>
  <div class="container">
    <h1>Welcome to the Node.js Blog API</h1>

    <section>
      <p>This project is a simple blogging backend built with Express.js and MongoDB. It covers authentication, blog post CRUD, and user profiles using modern Node.js practices.</p>
    </section>

    <section>
      <h2>🚀 Features</h2>
      <ul>
        <li>User Authentication (Register, Login, Logout, Reset Password, Google OAuth)</li>
        <li>JWT-based Authorization</li>
        <li>Blog Post CRUD</li>
        <li>User Profile Management</li>
        <li>Code Formatting with Prettier & ESLint</li>
      </ul>
    </section>

    <section>
      <h2>🗂 Project Structure</h2>
      <ul>
        <li><code>app.js</code> – Main entry file</li>
        <li><code>src/routes</code> – All route definitions</li>
        <li><code>src/controllers</code> – Controller logic</li>
        <li><code>src/services</code> – Business logic services</li>
        <li><code>src/utils</code> – Utility/helper functions</li>
      </ul>
    </section>

    <section>
      <h2>📬 API Endpoints</h2>
      <ul>
        <li><code>POST</code> /api/auth/register</li>
        <li><code>POST</code> /api/auth/login</li>
        <li><code>POST</code> /api/auth/logout</li>
        <li><code>POST</code> /api/auth/reset-password/request</li>
        <li><code>POST</code> /api/auth/reset-password</li>
        <li><code>POST</code> /api/auth/google</li>
        <li><code>GET</code> /api/profile</li>
        <li><code>PUT</code> /api/profile</li>
        <li><code>GET</code> /posts</li>
        <li><code>GET</code> /posts/:id</li>
        <li><code>POST</code> /posts</li>
        <li><code>PUT</code> /posts/:id</li>
        <li><code>DELETE</code> /posts/:id</li>
      </ul>
    </section>

    <footer>
      Node.js Backend Team
    </footer>
  </div>

  <!-- jQuery (optional, if you're using jQuery) -->
  <script src="https://code.jquery.com/jquery-3.6.0.min.js"></script>

  <!-- Bootstrap JS (required for Bootstrap components) -->
  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
